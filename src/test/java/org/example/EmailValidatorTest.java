package org.example;
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

public class EmailValidatorTest {
   /* @Test
    public void testValidEmail() {
        EmailValidator validator = new EmailValidator();
        assertTrue(validator.isValidEmail("winston@codechallenge.com"));
    }
    @Test
    public void testValidEmailWithSpecialCharacter() {
        EmailValidator validator = new EmailValidator();
        assertTrue(validator.isValidEmail("bella-@codechallenge.com"));
        assertTrue(validator.isValidEmail("sally.ctl@codechallenge.com"));
    }
    @Test
    public void testInvalidEmailPrefix() {
        EmailValidator validator = new EmailValidator();
        assertFalse(validator.isValidEmail("123john.doe@codechallenge.com"));
        assertFalse(validator.isValidEmail(".david@codechallenge.com"));
        assertFalse(validator.isValidEmail("quarz#2020@codechallenge.com"));
        assertFalse(validator.isValidEmail("jonathanisgreat"));
    }
    @Test
    public void testInvalidEmailDomain() {
        EmailValidator validator = new EmailValidator();
        assertFalse(validator.isValidEmail("david19@cdchallenge.com"));
        assertFalse(validator.isValidEmail("john.doe@test.codechallenge.com"));
        assertFalse(validator.isValidEmail("john.doe@invalidcodechallenge.com"));
        assertFalse(validator.isValidEmail("valid_email@test.com"));
    }*/
}

